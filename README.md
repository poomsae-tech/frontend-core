# Frontend Core — Poomsae Tech

## 🚀 Технический стек

- **React 19** — библиотека для создания пользовательских интерфейсов
- **TypeScript** — статическая типизация
- **Vite** — сборщик и dev-сервер
- **Tailwind CSS** — utility-first CSS фреймворк
- **shadcn/ui** — компоненты на основе Radix UI
- **React Router DOM** — роутинг для SPA
- **Zustand** — управление глобальным состоянием
- **Axios** — HTTP клиент
- **Vitest** — юнит-тестирование
- **Testing Library** — тестирование React компонентов
- **Playwright** — E2E тестирование (инфраструктура готова, тесты пишутся по мере необходимости)
- **Storybook** — разработка и документация компонентов
- **ESLint** — статический анализ кода

---

## 📁 Структура проекта

```
src/
├── assets/          # Статические ресурсы
├── components/
│   ├── ui/          # shadcn/ui — не редактировать вручную
│   └── ...          # Переиспользуемые компоненты проекта
├── features/        # Фичи: каждая папка содержит типы, колонки/логику и страницу
├── pages/           # Точки входа для роутера (тонкие, без логики)
├── router/          # Конфигурация маршрутов
├── lib/             # Утилиты (cn и др.)
├── stories/         # Storybook onboarding-примеры (временно)
└── tests/
    ├── unit/        # Vitest + Testing Library (зеркалит структуру src/)
    └── e2e/         # Playwright (по мере роста приложения)
```

---

## 🖥️ Команды

### Разработка

```bash
bun run dev          # dev-сервер на localhost:5173
bun run build        # сборка для продакшна (tsc + vite build)
bun run preview      # превью продакшн-сборки
bun run lint         # проверка ESLint
```

### Тестирование

```bash
bun run test         # юнит-тесты в watch-режиме
bun run test --run   # юнит-тесты однократно (CI)
bun run test:ui      # юнит-тесты с визуальным UI Vitest
bun run test:storybook  # тесты Storybook через Playwright
```

> ⚠️ Важно: использовать `bun run test`, а не `bun test` — второй вариант запускает встроенный тест-раннер Bun, который не использует конфиг Vitest и jsdom.

### Storybook

```bash
bun run storybook          # запуск на localhost:6006
bun run build-storybook    # статическая сборка
```

---

## 🏗️ Архитектурные принципы

**`features/`** — основная единица организации кода. Каждая фича содержит типы, логику отображения и сам компонент страницы. Когда появятся API-вызовы и сторы — они тоже живут здесь.

**`pages/`** — только реэкспорты или минимальные обёртки для роутера. Вся логика — в `features/`.

**`components/`** — переиспользуемые компоненты, не привязанные к конкретной фиче. `ui/` — исключительно shadcn, не редактировать вручную.

**`tests/`** — тесты отдельной папкой, не рядом с исходниками, чтобы не смешивать с тестами Storybook.

---

## ➕ Добавление shadcn компонентов

```bash
bunx shadcn add <component>
# например:
bunx shadcn add dialog
bunx shadcn add select
```

Компоненты появятся в `src/components/ui/`.