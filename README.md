# Frontend Core - Poomsae Tech

## 🚀 Технический стек

- **React 19** - библиотека для создания пользовательских интерфейсов
- **TypeScript** - статическая типизация JavaScript
- **Vite** - быстрый сборщик и dev-сервер
- **Tailwind CSS** - utility-first CSS фреймворк
- **shadcn/ui** - компоненты на основе Radix UI
- **React Router Dom** - роутинг для SPA
- **Zustand** - легковесное управление состоянием
- **Axios** - HTTP клиент для API запросов
- **Storybook** - инструмент для разработки компонентов
- **Vitest** - быстрое юнит-тестирование
- **Playwright** - E2E тестирование
- **ESLint** - статический анализ кода

## 📁 Структура проекта

```
frontend-core/
├── public/                     # Статические файлы
├── src/
│   ├── assets/                 # Ресурсы (изображения, иконки)
│   ├── components/             # Переиспользуемые компоненты
│   │   ├── ui/                 # Базовые UI компоненты (shadcn/ui)
│   │   └── custom/             # Кастомные компоненты проекта (либо тут будут вложенные папки под фичи, либо выше в src/features, пока думаю)
│   ├── hooks/                  # Кастомные React хуки
│   ├── layouts/                # Компоненты разметки страниц (изначально скорее всего не будет, если поймем что лейаутов много - создадим и перенесем)
│   ├── pages/                  # Страницы приложения
│   ├── services/               # API сервисы и внешние интеграции
│   ├── stores/                 # Zustand stores (глобальное состояние)
│   ├── types/                  # TypeScript типы и интерфейсы (важно: общие!)
│   ├── lib/                    # Утилитные функции
│   ├── constants/              # Константы приложения
│   ├── config/                 # Конфигурационные файлы
│   └── stories/                # Storybook stories (позже удалю и скорее всего будем писать рядом с компонентами - кажется что будет проще, пока думаю) - пока что будет, что-бы можно было подглядывать структуру
├── .storybook/                 # Конфигурация Storybook
├── components.json             # Конфигурация shadcn/ui
├── vite.config.ts              # Конфигурация Vite
├── tsconfig.json               # Конфигурация TypeScript
├── tailwind.config.js          # Конфигурация Tailwind CSS
├── eslint.config.js            # Конфигурация ESLint
└── package.json                # Зависимости и скрипты
```

## 🏗️ Архитектурные принципы

- **Components** - только UI логика
- **Hooks** - бизнес-логика и состояние
- **Services** - внешние API и интеграции
- **Stores** - глобальное состояние
- **Lib** - чистые функции без побочных эффектов

##    Тестирование

###    Тесты будут лежать рядом с компонентами (в папке tests - unit-тесты точно пишем и мб интеграционные, посмотрим, e2e как-будто нет, скриншотные сомнительно но окэй)
